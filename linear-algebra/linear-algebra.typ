#import "@local/booxtyp:0.0.2": *
#set math.equation(numbering: "(1)")

#show: book

// Here begins the main text of the book

= Vector Spaces

== Definition of Vector Spaces

$ alpha bold(v) in V $ <eq:1>

As shown in @eq:1 is a vector in the vector space $ V $.

= Operators on Complex Vector Spaces

== Generalized Eigenvectors

#math.equation(block: true, numbering: "(i)")[A = 1]

$ A $

#counter(math.equation).update(0)

$ B $

#index(entry: "Vector")[vector]
#index[Vector Space]

#columns(2)[
  #make-index()
]